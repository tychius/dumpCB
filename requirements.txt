# Core logic dependencies
pathspec>=0.12.1
chardet>=5.0.0

# UI dependencies (PySide6)
PySide6>=6.6.0 # Using a recent version, adjust if needed

# Image handling for icons (required by UI)
Pillow>=10.0.0 